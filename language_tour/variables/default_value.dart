void main(List<String> args) {
  // int? lineCount;
  // assert(lineCount == null);

  // int lineCount = 0;

  // int lineCount;
  // if (weLikeToCount) {
  //   lineCount = countLines();
  // } else {
  //   lineCount = 0;
  // }
  // print(lineCount);
}
