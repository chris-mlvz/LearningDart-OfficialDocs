/// To specify types when using a constructor, put the angle brackets (<...>).
// var nameSet = Set<String>.from(names);

/// The following code creates a map that has integer keys and values of type
/// View
// var views = Map<int, View>();
