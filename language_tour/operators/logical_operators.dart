void main(List<String> args) {
  // ! Not.
  // || OR.
  // && AND.

  // Example of using logical operators.
  var col = 0;
  var done = false;
  if (!done && (col == 0 || col == 3)) {
    // ...Do something...
  }
}
