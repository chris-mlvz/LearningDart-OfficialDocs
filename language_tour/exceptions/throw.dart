void main(List<String> args) {
  // * Throwing or raising, an exeption.
  // throw FormatException('Expected at least 1 section');

  // * throw aritary objects
  // throw 'Out of llamas!';

  // * Productionquality code usually implement [Error] or [Exception].

  // * Throw is an expresion, you can throw exeptions in => statements
  // void distanceTo(Point other) => throw UnimplementedError();
}
